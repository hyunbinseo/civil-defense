<urlset xmlns='http://www.sitemaps.org/schemas/sitemap/0.9'>
	{% for location in pathnames %}
		{{ product.title }}
		<url>
			<loc>
				{{- domain -}}
				{{- location -}}
			</loc>
			<lastmod>{{ lastmod }}</lastmod>
		</url>
	{% endfor %}
</urlset>
